# explore-lens

A lightweight codebase exploration tool. Run `make` to see project stats and
generate an overview report.

## Usage

```bash
make
```

Prints a project overview including file counts, dependency list, and module
descriptions to stdout.
